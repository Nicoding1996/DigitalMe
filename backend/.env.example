# Google Gemini API Configuration
GEMINI_API_KEY=AIza-your-api-key-here

# Server Configuration
PORT=3001

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Gemini Model Configuration
GEMINI_MODEL=gemini-2.0-flash-exp

# Google OAuth Configuration
# Obtain these credentials from Google Cloud Console (https://console.cloud.google.com)
# See GMAIL_SETUP_GUIDE.md for detailed setup instructions
# 
# Required OAuth 2.0 Scopes:
# - https://www.googleapis.com/auth/gmail.readonly (restricted to Sent folder only)
#
# Security Notes:
# - Never commit actual credentials to version control
# - Use different credentials for development and production
# - Rotate credentials if compromised
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth Redirect URI
# Must match exactly with the authorized redirect URI in Google Cloud Console
# Development: http://localhost:3001/api/auth/gmail/callback
# Production: https://yourdomain.com/api/auth/gmail/callback
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/gmail/callback

# Token Encryption Key
# Used to encrypt OAuth tokens at rest using AES-256-GCM
# Generate a secure 32-byte hex key using:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this key secret and never share it
TOKEN_ENCRYPTION_KEY=your-64-character-hex-key-here

# Gmail API Configuration
# Maximum number of emails to retrieve from user's Sent folder
# Default: 200 (recommended for comprehensive style analysis)
# Range: 50-500 (higher values increase processing time)
GMAIL_MAX_EMAILS=200

# Number of emails to fetch per Gmail API batch request
# Default: 50 (optimal for Gmail API quota management)
# Range: 10-100 (lower values = more API calls, higher values = larger responses)
# Note: Gmail API has a quota of 1 billion quota units per day
#       Each batch request consumes approximately 5-10 quota units
GMAIL_BATCH_SIZE=50
